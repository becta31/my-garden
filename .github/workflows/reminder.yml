name: Garden Daily Reminder

on:
  schedule:
    - cron: '0 8 * * *'
  workflow_dispatch:

jobs:
  send_reminder:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          pip install requests google-genai openai

      # ✅ Восстанавливаем last_weather.json между запусками
      - name: Restore weather cache
        uses: actions/cache@v4
        with:
          path: last_weather.json
          key: garden-weather-cache
          restore-keys: |
            garden-weather-cache

      - name: Run reminder script
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          OPENWEATHER_API_KEY: ${{ secrets.OPENWEATHER_API_KEY }}
          HF_API_TOKEN: ${{ secrets.HF_API_TOKEN }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          CITY_NAME: ${{ secrets.CITY_NAME }}
        run: python send_tasks.py

      # ✅ Сохраняем last_weather.json (чтобы завтра была “дельта”)
      - name: Save weather cache
        uses: actions/cache@v4
        with:
          path: last_weather.json
          key: garden-weather-cache

      - name: Notify on Failure
        if: failure()
        run: |
          curl -s -X POST https://api.telegram.org/bot${{ secrets.TELEGRAM_TOKEN }}/sendMessage \
          -d chat_id=${{ secrets.TELEGRAM_CHAT_ID }} \
          -d text="⚠️ ОШИБКА: Бот упал! Проверь логи в GitHub Actions."
